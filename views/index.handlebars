{{> _hero}}
<nav class="panel has-text-centered">
  <p class="panel-heading">
    Polls
  </p>
  {{#each polls}}

  <a id="center" class="panel-block is-active" href="/dashboard/{{_id}}">
    {{title}}
  </a>

  {{/each}}
</nav>